<?xml version="1.0" encoding="UTF-8"?>

<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="
             http://maven.apache.org/POM/4.0.0
             https://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <!-- ======================================================== -->
    <!-- IDENTIDAD DEL PROYECTO -->
    <!-- ======================================================== -->

    <groupId>com.alucardstudio</groupId>
    <artifactId>RellenadorEX17</artifactId>
    <version>1.2.0</version>

    <name>Rellenador EX-17</name>


    <!-- ======================================================== -->
    <!-- PROPIEDADES -->
    <!-- ======================================================== -->

    <properties>

        <project.build.sourceEncoding>
            UTF-8
        </project.build.sourceEncoding>

        <maven.compiler.release>
            21
        </maven.compiler.release>

        <!--
            JavaFX alineado con Java 21.
        -->
        <javafx.version>
            21.0.6
        </javafx.version>

        <pdfbox.version>
            3.0.5
        </pdfbox.version>

    </properties>


    <!-- ======================================================== -->
    <!-- DEPENDENCIAS -->
    <!-- ======================================================== -->

    <dependencies>

        <!-- JavaFX Controls -->
        <dependency>
            <groupId>org.openjfx</groupId>
            <artifactId>javafx-controls</artifactId>
            <version>${javafx.version}</version>
        </dependency>

        <!-- JavaFX FXML -->
        <dependency>
            <groupId>org.openjfx</groupId>
            <artifactId>javafx-fxml</artifactId>
            <version>${javafx.version}</version>
        </dependency>

        <!-- Apache PDFBox -->
        <dependency>
            <groupId>org.apache.pdfbox</groupId>
            <artifactId>pdfbox</artifactId>
            <version>${pdfbox.version}</version>
        </dependency>

    </dependencies>


    <!-- ======================================================== -->
    <!-- COMPILACIÓN -->
    <!-- ======================================================== -->

    <build>

        <plugins>

            <!-- ================================================= -->
            <!-- COMPILADOR JAVA -->
            <!-- ================================================= -->

            <plugin>

                <groupId>
                    org.apache.maven.plugins
                </groupId>

                <artifactId>
                    maven-compiler-plugin
                </artifactId>

                <version>
                    3.13.0
                </version>

                <configuration>

                    <release>
                        21
                    </release>

                </configuration>

            </plugin>


            <!-- ================================================= -->
            <!-- JAVAFX -->
            <!-- ================================================= -->

            <plugin>

                <groupId>
                    org.openjfx
                </groupId>

                <artifactId>
                    javafx-maven-plugin
                </artifactId>

                <version>
                    0.0.8
                </version>

                <configuration>

                    <mainClass>
                        com.alucardstudio.rellenadorex17.Launcher
                    </mainClass>

                </configuration>

            </plugin>

        </plugins>

    </build>

</project>